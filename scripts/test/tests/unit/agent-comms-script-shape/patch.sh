#!/usr/bin/env bash
# Patch: no setup required. The script-shape test exercises the
# features/agent-comms/code/ scripts in-place; it doesn't need a
# sandbox project. SANDBOX is unused.

set -uo pipefail

echo "  agent-comms-script-shape patch applied (no-op; scripts tested in-place)"
